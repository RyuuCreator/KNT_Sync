<form method="post" action="{{ path('app_admin_ambiance_delete', {'slug': ambiance.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ambiance.id) }}">
    <button class="btn-delete">Supprimer</button>
</form>