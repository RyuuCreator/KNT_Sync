{% extends 'base.html.twig' %}

{% block title %}Ambiance{% endblock %}

{% block body %}
    <div class="container-content">
        <h1>Ambiance</h1>
        <small>Retourner sur votre espace <a href="{{ path("app_admin") }}"><u>administrateur</u></a></small>

        <div class="content-ambiance">
            <img src="{{ ambiance.picture }}" alt="">
            <h1>{{ ambiance.label }}</h1>
        </div>

        <a href="{{ path('app_ambiance_index') }}">back to list</a>

        <a href="{{ path('app_ambiance_edit', {'id': ambiance.id}) }}">edit</a>

        {{ include('admin/ambiance/_delete_form.html.twig') }}
    </div>
{% endblock %}
