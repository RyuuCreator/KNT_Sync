{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <div class="container-content">
        <h1>Category</h1>
        <small>Retourner sur votre espace <a href="{{ path("app_admin") }}"><u>administrateur</u></a></small>

        <div class="content-category">
            <img src="{{ category.picture }}" alt="">
            <h1>{{ category.label }}</h1>
        </div>

        <a class="btn-edit" href="{{ path('app_admin_category_edit', {'slug': category.slug}) }}">Modifier</a>

        {{ include('admin/category/_delete_form.html.twig') }}

        <a href="{{ path('app_admin_category_index') }}">Retour à la liste</a>
    </div>
{% endblock %}
